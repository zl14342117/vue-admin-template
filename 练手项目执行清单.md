# LiteOps 练手项目执行清单

> 基于 `vue-admin-template` + `vue-admin-template-server` 的前后端分离练手项目  
> 目标：通过 6 个阶段，从零搭建一个可运行的轻量运营管理系统

---

## 一、项目概览

### 1.1 系统定位

**LiteOps** —— 轻量运营管理系统，覆盖后台管理最常见的业务能力：

| 模块 | 说明 | 练手重点 |
|------|------|----------|
| 工作台 | 数据概览、快捷入口 | 卡片布局、ECharts 图表 |
| 系统管理 | 用户、角色 | CRUD、分页、弹窗表单 |
| 客户管理 | 客户信息维护 | 搜索筛选、状态标签 |
| 商品管理 | 商品 SKU 维护 | 表单校验、图片上传 |
| 订单管理 | 订单列表与详情 | 多条件查询、详情页 |
| 数据报表 | 销售趋势统计 | 图表、日期范围选择 |

### 1.2 技术栈

| 层级 | 技术 |
|------|------|
| 前端 | Vue 2 + Vue Router + Vuex + Element UI + Axios |
| 后端 | Java 8 + Spring Boot 2.7 + 内存数据（后期可换 H2/MySQL） |
| 工程 | `vue-admin-template`（前端） / `vue-admin-template-server`（后端） |

### 1.3 目录规划（目标结构）

```text
vue-admin-template/src/
├── api/
│   ├── user.js          # 已有：登录鉴权
│   ├── customer.js      # 待建：客户
│   ├── product.js       # 待建：商品
│   └── order.js         # 待建：订单
├── views/
│   ├── login/           # 已有
│   ├── dashboard/       # 已有（工作台）
│   ├── system/
│   │   ├── user/        # 待建：用户管理
│   │   └── role/        # 待建：角色管理
│   ├── customer/        # 待建：客户管理
│   ├── product/         # 待建：商品管理
│   ├── order/           # 待建：订单管理
│   └── report/          # 待建：数据报表
└── router/index.js      # 按模块逐步注册路由

vue-admin-template-server/src/main/java/com/demo/admin/
├── controller/          # 每个模块一个 Controller
├── service/impl/          # 业务逻辑
├── pojo/dto/              # 入参
└── pojo/vo/               # 出参
```

---

## 二、环境准备

- [x] **P0-1** 克隆 `vue-admin-template` 并 `npm install`
- [x] **P0-2** 使用 Node 16 启动前端（`nvm use 16 && npm run dev`）
- [x] **P0-3** 清空模板 Demo 页面（Example / Form / Nested / External Link）
- [ ] **P0-4** 启动后端 `vue-admin-template-server`（端口 8090）

```bash
# 前端
cd vue-admin-template
nvm use 16
npm run dev          # http://localhost:9528

# 后端
cd vue-admin-template-server
mvn spring-boot:run  # http://localhost:8090
```

- [ ] **P0-5** 前端关闭 Mock，配置代理联调后端

编辑 `vue.config.js`：

```js
devServer: {
  port: 9528,
  proxy: {
    '/dev-api': {
      target: 'http://127.0.0.1:8090',
      changeOrigin: true,
      pathRewrite: { '^/dev-api': '' }
    }
  }
  // before: require('./mock/mock-server.js')  // 注释掉
}
```

- [ ] **P0-6** 验证登录：`admin` / 任意密码 → 进入工作台

---

## 三、阶段一：工作台 Dashboard

> 预计 0.5 天 | 熟悉页面布局与组件拼装

### 前端任务

- [ ] **S1-1** 安装 ECharts：`npm install echarts --save`
- [ ] **S1-2** 工作台顶部放 4 个统计卡片（客户数 / 商品数 / 今日订单 / 今日销售额）
- [ ] **S1-3** 中部放折线图：近 7 日订单趋势
- [ ] **S1-4** 底部放快捷入口按钮（跳转各业务模块）

### 后端任务

- [ ] **S1-5** 新增 `DashboardController`，路径 `/vue-admin-template/dashboard/summary`
- [ ] **S1-6** 返回 `DashboardSummaryVO`：统计数字 + 趋势数组

### 验收标准

- 登录后工作台展示真实接口数据（非写死）
- 图表可随接口数据渲染

---

## 四、阶段二：用户管理

> 预计 1 天 | 掌握标准 CRUD 页面模式（后续模块可复用）

### 前端任务

- [ ] **S2-1** 新建 `src/api/system/user.js`
- [ ] **S2-2** 新建 `src/views/system/user/index.vue`
- [ ] **S2-3** 页面结构：搜索栏 + 表格 + 分页 + 新增/编辑弹窗
- [ ] **S2-4** 表格列：用户名、昵称、角色、状态、创建时间、操作（编辑/删除）
- [ ] **S2-5** 注册路由：

```js
{
  path: '/system',
  component: Layout,
  redirect: '/system/user',
  meta: { title: '系统管理', icon: 'el-icon-setting' },
  children: [
    {
      path: 'user',
      name: 'SystemUser',
      component: () => import('@/views/system/user/index'),
      meta: { title: '用户管理', icon: 'user' }
    }
  ]
}
```

### 后端任务

- [ ] **S2-6** 新建 `SysUserController`，基础路径 `/vue-admin-template/system/user`
- [ ] **S2-7** 实现接口：

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/list` | 分页列表，参数 `page` `limit` `keyword` |
| GET | `/{id}` | 详情 |
| POST | `/` | 新增 |
| PUT | `/{id}` | 编辑 |
| DELETE | `/{id}` | 删除 |

- [ ] **S2-8** 内存 List 存储用户数据，初始化 5 条模拟数据

### 验收标准

- 列表分页正常
- 新增/编辑表单校验（用户名必填、不可重复）
- 删除有二次确认

---

## 五、阶段三：角色管理

> 预计 0.5 天 | 理解下拉关联、简单权限模型

### 前端任务

- [ ] **S3-1** 新建 `src/views/system/role/index.vue`
- [ ] **S3-2** 角色列表：角色名、角色标识（code）、描述、操作
- [ ] **S3-3** 用户管理「角色」字段改为下拉，数据来自角色列表接口
- [ ] **S3-4** 注册路由 `system/role`

### 后端任务

- [ ] **S3-5** 新建 `SysRoleController`，CRUD 接口
- [ ] **S3-6** 预置角色：`admin`（管理员）、`editor`（运营）

### 验收标准

- 用户表单可选择角色
- 角色 CRUD 完整可用

---

## 六、阶段四：客户管理

> 预计 1 天 | 搜索筛选 + 状态管理

### 前端任务

- [ ] **S4-1** 新建 `src/api/customer.js` + `src/views/customer/index.vue`
- [ ] **S4-2** 搜索：客户名称、手机号、状态（启用/禁用）
- [ ] **S4-3** 表格列：客户编号、名称、联系人、手机、状态、创建时间
- [ ] **S4-4** 状态用 `el-tag` 区分颜色
- [ ] **S4-5** 注册路由：

```js
{
  path: '/customer',
  component: Layout,
  children: [{
    path: 'index',
    name: 'Customer',
    component: () => import('@/views/customer/index'),
    meta: { title: '客户管理', icon: 'el-icon-user' }
  }]
}
```

### 后端任务

- [ ] **S4-6** 新建 `CustomerController`，完整 CRUD + 分页搜索
- [ ] **S4-7** 客户编号自动生成（如 `C20260001`）

### 验收标准

- 多条件搜索生效
- 启用/禁用状态可切换

---

## 七、阶段五：商品管理

> 预计 1 天 | 表单校验 + 枚举状态

### 前端任务

- [ ] **S5-1** 新建 `src/api/product.js` + `src/views/product/index.vue`
- [ ] **S5-2** 字段：商品名称、编码、分类、单价、库存、上架状态
- [ ] **S5-3** 表单校验：名称必填、单价 > 0、库存 >= 0
- [ ] **S5-4** 分类用下拉（数码 / 日用 / 食品），可先写死枚举
- [ ] **S5-5** 注册路由 `product/index`

### 后端任务

- [ ] **S5-6** 新建 `ProductController`，CRUD + 分页
- [ ] **S5-7** 上架/下架状态字段 `status: 0|1`

### 验收标准

- 表单校验提示正确
- 上架商品在订单模块可选

---

## 八、阶段六：订单管理

> 预计 1.5 天 | 复杂查询 + 详情页 + 模块联动

### 前端任务

- [ ] **S6-1** 新建 `src/api/order.js`
- [ ] **S6-2** 列表页 `src/views/order/index.vue`：订单号、客户、金额、状态、下单时间
- [ ] **S6-3** 详情页 `src/views/order/detail.vue`：订单头 + 商品明细表格
- [ ] **S6-4** 新建订单弹窗：选择客户 + 选择商品（多选）+ 自动计算总金额
- [ ] **S6-5** 订单状态：待支付 / 已支付 / 已取消（`el-tag` 区分）
- [ ] **S6-6** 注册路由（列表 + 详情，详情 `hidden: true`）

### 后端任务

- [ ] **S6-7** 新建 `OrderController`
- [ ] **S6-8** 接口：列表分页、详情、创建、取消
- [ ] **S6-9** 创建订单时校验商品库存并扣减（内存操作即可）

### 验收标准

- 创建订单后列表可见
- 详情页展示商品明细
- 取消订单后状态变更

---

## 九、阶段七：数据报表（进阶）

> 预计 1 天 | ECharts + 日期筛选

### 前端任务

- [ ] **S7-1** 新建 `src/views/report/index.vue`
- [ ] **S7-2** 日期范围选择器（默认近 30 天）
- [ ] **S7-3** 柱状图：每日销售额
- [ ] **S7-4** 饼图：商品分类销售占比
- [ ] **S7-5** 注册路由 `report/index`

### 后端任务

- [ ] **S7-6** 新建 `ReportController`，`/vue-admin-template/report/sales`
- [ ] **S7-7** 根据订单数据聚合统计（内存计算）

### 验收标准

- 切换日期范围，图表数据联动更新

---

## 十、进阶挑战（可选）

完成基础 7 阶段后，可继续挑战：

| 编号 | 任务 | 难度 |
|------|------|------|
| E-1 | 接入 H2 或 MySQL，替换内存数据 | ⭐⭐ |
| E-2 | 引入 MyBatis-Plus，写真实 SQL | ⭐⭐⭐ |
| E-3 | 登录 Token 改为 JWT | ⭐⭐ |
| E-4 | 路由权限：不同角色看到不同菜单 | ⭐⭐⭐ |
| E-5 | 文件上传：商品图片 | ⭐⭐ |
| E-6 | 导出 Excel：客户列表 / 订单列表 | ⭐⭐ |
| E-7 | 对照 `dc-portal-server` 的 `LeadTimeReportController` 写一个报表接口 | ⭐⭐⭐ |

---

## 十一、开发规范建议

### 11.1 前端

1. **一个模块一对文件**：`api/xxx.js` + `views/xxx/index.vue`
2. **列表页统一结构**：`搜索表单 → 操作按钮 → el-table → el-pagination`
3. **弹窗表单**：新增/编辑共用一个 `el-dialog`，用 `isEdit` 区分
4. **接口响应**：统一走 `request.js`，`code === 20000` 为成功

### 11.2 后端

1. **分层**：Controller → Service → 数据层（先内存，后数据库）
2. **统一响应**：使用 `ApiResponse<T>`，成功 `code = 20000`
3. **命名对照 dc-portal-server**：

| 本项目的类 | dc-portal-server |
|-----------|------------------|
| `XxxController` | `XxxController` |
| `XxxService` / `XxxServiceImpl` | 同名 |
| `XxxVO` / `XxxDTO` | `pojo/vo` / `pojo/dto` |

### 11.3 Git 提交建议

每完成一个阶段提交一次，便于回滚：

```text
feat: 阶段一 - 工作台 Dashboard
feat: 阶段二 - 用户管理 CRUD
feat: 阶段三 - 角色管理
...
```

---

## 十二、常见问题

| 问题 | 排查 |
|------|------|
| 前端白屏 / 路由 404 | 检查 `router/index.js` 路径与 `views/` 文件是否对应 |
| 接口 404 | 确认 proxy 配置、后端路径前缀 `/vue-admin-template/...` |
| 登录失败 code 60204 | 用户名必须是 `admin` 或 `editor` |
| Node 版本报错 | 使用 `nvm use 16`，不要用 Node 22 |
| Mock 与后端冲突 | 注释 `vue.config.js` 中的 `before: mock-server` |

---

## 十三、进度追踪

| 阶段 | 模块 | 状态 | 完成日期 |
|------|------|------|----------|
| P0 | 环境准备 & 清空 Demo | ✅ 进行中 | |
| S1 | 工作台 Dashboard | ⬜ 待开始 | |
| S2 | 用户管理 | ⬜ 待开始 | |
| S3 | 角色管理 | ⬜ 待开始 | |
| S4 | 客户管理 | ⬜ 待开始 | |
| S5 | 商品管理 | ⬜ 待开始 | |
| S6 | 订单管理 | ⬜ 待开始 | |
| S7 | 数据报表 | ⬜ 待开始 | |

---

> 建议从 **阶段一（工作台）** 开始，每完成一个阶段打勾 ✅，循序渐进即可掌握前后端分离开发的完整流程。
